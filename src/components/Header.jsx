function Header() {
    return (
        <header>
            <h1 className="title">Tenzies</h1>
            <p className="instructions">Click on each die to freeze its current number, and keep rolling until all dice are the same!</p>
        </header>
    )
}

export default Header