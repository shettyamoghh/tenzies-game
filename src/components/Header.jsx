function Header() {
    return (
        <header>
            <h1 className="title">Tenzies</h1>
            <p className="instructions">Keep rolling until all dice are the same! Click on each die to freeze its current number</p>
        </header>
    )
}

export default Header